header {
  height: 1em;
  padding: 1em;
  margin-bottom: 1em;
  background: #0099cc;
  background-image: url('<%#= image_path(gradient.png)%>');
}

header span {
  float: right;
}

header span .button_to {
  display: inline;
}

div#content {
  min-height: 400px;
}

div#error-explanation {
    color: red;
}

footer {
  height: 1em;
  padding: 1em;
  margin-top: 1em;
  background: #0099cc;
  background-image: url('<%#= image_path(gradient.png)%>');
}
